export enum Health {
  full = 'full',
  damaged = 'damaged',
  nearlydead = 'nearlydead',
  dead = 'dead',
}

export enum selection {
  current = 'current',
  selected = 'selected',
  none = 'none',
}
