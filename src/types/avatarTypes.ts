export enum AvatarHealth {
  full = 'full',
  damaged = 'damaged',
  nearlydead = 'nearlydead',
  dead = 'dead',
}

export enum Selection {
  current = 'current',
  selected = 'selected',
  none = 'none',
}
